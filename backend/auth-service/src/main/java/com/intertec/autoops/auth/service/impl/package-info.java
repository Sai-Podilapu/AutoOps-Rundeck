/**
 * Service implementations.
 *
 * <p>Per the AutoOps convention, services are concrete classes and an
 * interface is introduced only when more than one implementation exists;
 * implementations then live in this package (e.g.
 * {@code NotificationService} -> {@code impl/SendGridNotificationService}).
 * All current services have a single implementation, so they sit directly in
 * {@code com.intertec.autoops.auth.service}.
 */
package com.intertec.autoops.auth.service.impl;
