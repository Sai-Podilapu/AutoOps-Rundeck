# Dev profile: local MySQL runs on host port 3307 (see auth-service compose).
logging:
  level:
    com.intertec.autoops.core: DEBUG

spring:
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3307}/${DB_NAME:autoops_core}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
